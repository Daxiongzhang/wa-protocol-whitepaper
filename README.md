# WA资料库文件

> 工业级多语言Web应用 - 基于React + TypeScript + Tailwind CSS构建

## 🚀 项目概述

WA资料库文件是一个现代化的多语言Web应用，采用工业级开发标准和最佳实践构建。项目支持7种语言（中文、英文、印尼语、泰语、越南语、韩语、日语），提供优雅的用户体验和卓越的性能表现。

### ✨ 核心特性

- 🌍 **多语言支持** - 支持7种主要亚洲语言
- 🎨 **现代化UI** - 基于Tailwind CSS的精美设计
- ⚡ **高性能** - 代码分割、懒加载、性能优化
- 🛡️ **类型安全** - 完整的TypeScript类型系统
- 🔧 **工业级架构** - 模块化、可维护的代码结构
- 📱 **响应式设计** - 完美适配各种设备
- 🎯 **错误边界** - 全局错误处理和优雅降级
- 🧪 **代码质量** - ESLint + Prettier + Husky

## 🏗️ 技术栈

### 前端框架
- **React 18** - 现代化前端框架
- **TypeScript** - 类型安全的JavaScript
- **Vite** - 快速的构建工具

### UI & 样式
- **Tailwind CSS** - 实用优先的CSS框架
- **Radix UI** - 无障碍的组件库
- **Lucide React** - 精美的图标库

### 开发工具
- **ESLint** - 代码质量检查
- **Prettier** - 代码格式化
- **Husky** - Git钩子管理
- **TypeScript** - 静态类型检查

## 📁 项目结构

```
src/
├── components/          # 可复用组件
│   ├── ui/             # 基础UI组件
│   ├── ErrorBoundary.tsx
│   ├── Header.tsx
│   ├── Footer.tsx
│   └── index.ts
├── pages/              # 页面组件
│   ├── HomePage.tsx
│   ├── ResourcesPage.tsx
│   ├── ToolsPage.tsx
│   └── ...
├── hooks/              # 自定义Hooks
│   ├── usePerformance.ts
│   └── ...
├── types/              # TypeScript类型定义
│   ├── common.types.ts
│   ├── app.types.ts
│   ├── content.types.ts
│   └── index.ts
├── data/               # 静态数据
│   ├── header-content.ts
│   ├── home-content.ts
│   └── ...
├── styles/             # 样式文件
├── utils/              # 工具函数
└── App.tsx             # 主应用组件
```

## 🚀 快速开始

### 环境要求

- Node.js >= 16.0.0
- npm >= 8.0.0

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

应用将在 http://localhost:5173 启动

### 生产构建

```bash
npm run build
```

### 预览构建

```bash
npm run preview
```

## 🎯 开发指南

### 代码规范

项目使用严格的代码规范：

- **ESLint** - 代码质量检查
- **Prettier** - 代码格式化
- **TypeScript** - 类型检查

```bash
# 检查代码规范
npm run lint

# 自动修复代码规范
npm run lint:fix

# 格式化代码
npm run format
```

### 组件开发

#### 组件结构

```typescript
import { memo } from 'react'
import type { ComponentProps } from '@/types'

interface Props extends ComponentProps {
  // 组件特定属性
}

function Component({ ...props }: Props) {
  // 组件逻辑
}

export const Component = memo(Component)
```

#### 最佳实践

- 使用 `memo` 优化性能
- 使用 `useCallback` 和 `useMemo` 避免不必要的重渲染
- 遵循单一职责原则
- 添加完整的TypeScript类型定义

### 状态管理

项目使用React内置状态管理，配合自定义Hooks：

```typescript
// 使用自定义Hook
const { state, actions } = useAppState()

// 性能优化Hook
const debouncedFn = useDebounce(callback, 300)
```

### 错误处理

使用ErrorBoundary进行全局错误捕获：

```typescript
<ErrorBoundary>
  <App />
</ErrorBoundary>
```

## 📊 性能优化

### 已实现的优化

- ✅ 代码分割和懒加载
- ✅ 组件级别的memo化
- ✅ 防抖和节流
- ✅ 图片懒加载
- ✅ 滚动性能优化
- ✅ 内存泄漏防护

### 性能监控

开发模式下自动启用性能监控：

```typescript
usePerformanceMonitor('ComponentName')
```

## 🌐 多语言支持

### 支持的语言

- 🇨🇳 中文 (zh)
- 🇺🇸 英文 (en)
- 🇮🇩 印尼语 (id)
- 🇹🇭 泰语 (th)
- 🇻🇳 越南语 (vi)
- 🇰🇷 韩语 (ko)
- 🇯🇵 日语 (ja)

### 添加新语言

1. 在 `types/common.types.ts` 中添加语言类型
2. 在各内容文件中添加翻译
3. 更新语言配置

## 🧪 测试

```bash
# 运行测试
npm test

# 测试覆盖率
npm run test:coverage
```

## 📦 部署

### 构建配置

项目已配置为在 `/v3` 路径下部署：

```typescript
// vite.config.ts
export default defineConfig({
  base: '/v3',
  // ...
})
```

### 部署到静态托管

1. 构建项目：`npm run build`
2. 将 `build` 目录部署到静态托管服务
3. 确保托管服务支持SPA路由

## 🤝 贡献指南

### 开发流程

1. Fork 项目
2. 创建功能分支：`git checkout -b feature/amazing-feature`
3. 提交更改：`git commit -m 'Add amazing feature'`
4. 推送分支：`git push origin feature/amazing-feature`
5. 提交Pull Request

### 提交规范

使用约定式提交格式：

- `feat:` 新功能
- `fix:` 修复bug
- `docs:` 文档更新
- `style:` 代码格式化
- `refactor:` 代码重构
- `perf:` 性能优化
- `test:` 测试相关
- `chore:` 构建工具或辅助工具的变动

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情

## 🔗 相关链接

- [Figma设计稿](https://www.figma.com/design/ZBjHTe8At5bdkLwjtEuoeL/WA%E8%B5%84%E6%96%99%E5%BA%93%E6%96%87%E4%BB%B6)
- [React文档](https://react.dev/)
- [TypeScript文档](https://www.typescriptlang.org/)
- [Tailwind CSS文档](https://tailwindcss.com/)

---

💡 **提示**: 这是一个工业级项目，遵循最佳实践和现代开发标准。如有问题，请查看文档或提交Issue。