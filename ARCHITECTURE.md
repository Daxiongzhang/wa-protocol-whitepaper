# 白皮书系统架构 - 数据层与视觉层分离

## 📋 概述

本项目实现了数据层与视觉层的清晰分离，提高了代码的可维护性和可扩展性。

## 🏗️ 架构设计

### 数据层 (Data Layer)
- **位置**: `/src/data/`
- **职责**: 管理配置数据、业务逻辑、图表定义
- **文件**:
  - `whitepaper-content.ts` - 白皮书内容数据
  - `chart-config.ts` - 图表配置和检测逻辑

### 视觉层 (Visual Layer)
- **位置**: `/src/components/`, `/src/pages/`
- **职责**: UI渲染、组件管理、用户交互
- **文件**:
  - `ChartManager.tsx` - 图表管理器
  - `WhitepaperPage.tsx` - 主页面组件
  - `OptimizedArchitectureImage.tsx` - 架构图组件
  - `FlowDiagramImage.tsx` - 流程图组件
  - `BusinessLoopImage.tsx` - 商业闭环图组件
  - `IncentiveStructureImage.tsx` - 激励体系图组件

## 🔧 核心组件

### ChartConfig (数据层)
```typescript
export interface ChartConfig {
  id: string;
  title: string;
  component: React.ComponentType<any>;
  keywords: string[];
  description: string;
}
```

### ChartRegistry (数据层)
- 图表组件注册器
- 动态组件管理
- 类型安全的组件获取

### ChartManager (视觉层)
- 图表组件渲染
- 自动组件注册
- 统一的图表管理接口

## 📊 图表配置

### 当前支持的图表
1. **架构总览图** - `architecture-diagram`
2. **流程结构图** - `flow-diagram`
3. **商业闭环总图** - `business-loop-diagram`
4. **激励体系结构图** - `incentive-structure-diagram`

### 添加新图表
1. **数据层**: 在 `chart-config.ts` 中添加配置
2. **视觉层**: 创建图表组件
3. **注册**: 在 `ChartManager` 中注册组件

## 🔄 工作流程

1. **初始化**: `useChartInitialization()` Hook 注册所有图表组件
2. **检测**: `detectChart()` 根据关键词检测图表类型
3. **渲染**: `ChartRenderer` 组件渲染对应图表
4. **管理**: `ChartManager` 提供统一的图表管理接口

## 🎯 优势

### 可维护性
- **单一职责**: 数据层专注配置，视觉层专注渲染
- **松耦合**: 通过接口和注册机制解耦
- **类型安全**: TypeScript 提供完整的类型检查

### 可扩展性
- **插件化**: 新图表可以轻松添加
- **配置驱动**: 通过配置文件管理图表
- **动态加载**: 支持按需加载图表组件

### 可测试性
- **独立测试**: 数据层和视觉层可以独立测试
- **Mock友好**: 易于创建测试替身
- **单元测试**: 每个组件都可以单独测试

## 📁 文件结构

```
src/
├── data/
│   ├── whitepaper-content.ts     # 白皮书内容
│   └── chart-config.ts           # 图表配置
├── components/
│   ├── ChartManager.tsx          # 图表管理器
│   ├── OptimizedArchitectureImage.tsx
│   ├── FlowDiagramImage.tsx
│   ├── BusinessLoopImage.tsx
│   └── IncentiveStructureImage.tsx
└── pages/
    └── WhitepaperPage.tsx        # 主页面
```

## 🚀 使用示例

### 添加新图表
```typescript
// 1. 数据层配置
export const CHART_CONFIGS: ChartConfig[] = [
  {
    id: 'new-chart',
    title: '新图表',
    keywords: ['新图表', '关键词'],
    description: '新图表描述',
    component: null as any,
  },
];

// 2. 视觉层组件
const NewChartImage = () => {
  return <img src="/v3/new-chart.png" alt="新图表" />;
};

// 3. 注册组件
ChartRegistry.register('new-chart', NewChartImage);
```

### 渲染图表
```typescript
// 在任何地方使用
const chartElement = ChartRenderer({ text: "新图表标题", index: 0 });
```

## 🔮 未来扩展

1. **主题系统**: 支持多种视觉主题
2. **国际化**: 多语言图表支持
3. **动画效果**: 图表加载和交互动画
4. **响应式设计**: 更好的移动端适配
5. **性能优化**: 图表懒加载和缓存机制
